cd - me permite cambiar de directorio
  cd "Carpeta con espacios"
  rm - elimina archivos
  rm - - nos permite eliminar carpetas y su contenido


git add .gitignore
  git add .
  git commit -m "actualizando espacio de encabezado"
  git push origin main